# PTO-WSP v9/v10 design reference

This folder preserves the historical **PTO-WSP** v9/v10 design documents.

- `docs/reference/pto-wsp/v9/` — v9-era docs (analysis/features/spec/implementation/etc.)
- `docs/reference/pto-wsp/v10/` — v10 living spec docs captured as reference

These docs are kept for historical context only; the active redo design lives in `docs/future-htp/`.
